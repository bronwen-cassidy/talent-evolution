CORE_DETAILS=SELECT * FROM CORE_DETAILS WHERE ID > 2
USERS=SELECT * FROM USERS WHERE ID > 2
LOGINS=SELECT * FROM LOGINS WHERE USER_ID > 2
APP_ROLES_USERS=SELECT * FROM APP_ROLES_USERS WHERE USER_ID > 2

NODES=SELECT * FROM NODES WHERE ID > 1

SUBJECTS
POSITIONS=SELECT * FROM POSITIONS WHERE NODE_ID > 1
ORGANIZATION_UNITS= SELECT * FROM ORGANIZATION_UNITS WHERE NODE_ID > 0

SUBJECT_ASSOCIATIONS
POSITION_ASSOCIATIONS

LOOKUP_VALUES=SELECT DISTINCT LV.* FROM LOOKUP_TYPES LT, LOOKUP_VALUES LV WHERE LV.TYPE_ID = LT.ID AND LT.TYPE = 'QUESTIONNAIRE'
LOOKUP_TYPES=SELECT * FROM LOOKUP_TYPES WHERE TYPE='QUESTIONNAIRE'
DYNAMIC_ATTRIBUTES=SELECT * FROM DYNAMIC_ATTRIBUTES WHERE QD_ID IS NOT NULL
DYNAMIC_ATTR_REFERENCES=SELECT * FROM DYNAMIC_ATTR_REFERENCES ORDER BY PARENT_MAPPING_ID DESC

NODE_DAS=SELECT ND.* FROM NODE_DAS ND, DYNAMIC_ATTRIBUTES DA WHERE ND.DA_ID = DA.ID AND DA.QD_ID IS NOT NULL
NODE_DA_FILES=SELECT NDF.* FROM NODE_DA_FILES NDF, NODE_DAS ND, DYNAMIC_ATTRIBUTES DA WHERE NDF.NODE_DA_ID = ND.ID AND ND.DA_ID = DA.ID AND DA.QD_ID IS NOT NULL

POPULATIONS=SELECT * FROM POPULATIONS WHERE ID > 0
METRICS=SELECT * FROM METRICS WHERE ID > 0
REPORTS=SELECT * FROM REPORTS WHERE ID > 0
REPORT_COLUMNS=SELECT * FROM REPORT_COLUMNS WHERE REP_ID > 0
COLUMN_DISPLAY_IMAGES

PERFORMANCE_REVIEWS
QUE_DEFINITIONS
QUE_DEFINITION_MODELS
QUE_WORKFLOWS

QUESTIONNAIRES
QUE_WF_PARTICIPANTS
